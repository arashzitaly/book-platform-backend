# Local Development Environment Variables
# Copy this to .env and fill in values

# Database (using local PostgreSQL container)
DATABASE_URL=Host=db;Port=5432;Database=bookplatform;Username=postgres;Password=postgres

# JWT Configuration
JWT_SECRET=LocalDevSecretKeyThatIsAtLeast32CharactersLong!

# Supabase (if using Supabase instead of local DB)
# DATABASE_URL=Host=db.dhrpvecosxeswehwbdzh.supabase.co;Port=5432;Database=postgres;Username=postgres;Password=YOUR_PASSWORD;SSL Mode=Require
